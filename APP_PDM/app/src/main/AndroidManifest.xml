<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Main Activity (Landing) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Menu Selection Activity -->
        <activity
            android:name=".ui.menu.MenuSelectionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Food Menu Activity -->
        <activity
            android:name=".ui.menu.FoodMenuActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- QR Code Activity -->
        <activity
            android:name=".ui.qr.QRCodeActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Map Activity -->
        <activity
            android:name=".ui.map.MapActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Request Info Activity -->
        <activity
            android:name=".ui.contact.RequestInfoActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Success Activity -->
        <activity
            android:name=".ui.common.SuccessActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.payment.PaymentSuccessActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
    </application>

</manifest>